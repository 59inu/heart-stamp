name: Daily AI Analysis

on:
  schedule:
    # 매일 새벽 2시 59분 (KST) = UTC 17:59
    - cron: '59 17 * * *'
  workflow_dispatch: # 수동 실행 가능

jobs:
  wake-server-for-ai-analysis:
    runs-on: ubuntu-latest
    steps:
      - name: Wake up server before AI analysis
        run: |
          echo "🤖 Waking up server at $(date) for AI analysis at 3:00 AM KST"
          response=$(curl -s -w "\n%{http_code}" ${{ secrets.RAILWAY_URL }}/health)

          http_code=$(echo "$response" | tail -n1)
          body=$(echo "$response" | head -n-1)

          echo "Response: $body"
          echo "HTTP Status: $http_code"

          if [ "$http_code" -eq 200 ]; then
            echo "✅ Server is awake and ready for AI analysis cron job"
          else
            echo "⚠️  Server responded with status $http_code"
          fi
